/**
 * LeetCode Solution #1
 *
 * This is where we shall define our Solution
 * to the LeetCode problem.
 *
 * Copyright @2023 Brian M'Ikiara
 *
 */
#include "./0-addTwoNumbers.h"

/**
  * 
 */

/**
 * addTwoNumbers - Function
 * @l1: First Linked List
 * @l2: Second Linked List
 *
 * Description: This program takes in two singly Linked
 * Lists and then returns the sum of the addition of
 * the numbers stored in each as a Linked List too.
 * Returns: Pointer to the new linked list; NULL, otherwise.
 */
struct ListNode* addTwoNumbers(struct ListNode* l1, struct ListNode* l2) {
  int sum = 0;
  struct ListNode* head = NULL, result = NULL;

  if (!l1 && !l2) return (NULL);
  while (l1)  
  return (result);
};
